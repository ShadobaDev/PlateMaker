cmake_minimum_required(VERSION 3.21)

# ---------------------------------------------------------------------------
# Unit tests for libplatemaker
# Stage 1: test targets will be added here as components are implemented.
# ---------------------------------------------------------------------------

# Prefer GoogleTest (available via vcpkg: "gtest")
# find_package(GTest CONFIG REQUIRED)

# Example test executable (uncomment when first tests are added):
# add_executable(test_scaler
#     test_scaler.cpp
# )
# target_link_libraries(test_scaler PRIVATE Platemaker::platemaker GTest::gtest_main)
# add_test(NAME ScalerTests COMMAND test_scaler)
